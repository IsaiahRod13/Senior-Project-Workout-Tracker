<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Default Theme -->
    <style name="Theme.RepSync" parent="android:Theme.Material.Light.NoActionBar" />

    <!-- Splash Theme -->
    <style name="Theme.RepSync.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/black</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/splash_icon</item>
        <item name="postSplashScreenTheme">@style/Theme.RepSync</item>
    </style>

</resources>